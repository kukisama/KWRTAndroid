# 代理协议选型经验（个人备忘）

> 适用场景：233boy 等订阅给的多种节点，路由器 / x86 PC 上怎么选。
> 沉淀自 2026-06-04 的 4K 卡顿排障实战。

---

## 一句话结论

> **链路好 + 强 CPU → Reality；链路差或被 QoS → Hysteria2；MIPS/ARM 弱路由 → 永远优先 TCP+ChaCha20 类（Reality / SS-chacha20 / Trojan-chacha20）。**

---

## 各协议本质对比

| 维度 | Hysteria2 | VLESS-Reality | Shadowsocks (ChaCha20) | Trojan | VMess/VLESS-WS-TLS |
|---|---|---|---|---|---|
| 传输层 | QUIC / UDP | TCP | TCP/UDP | TCP | TCP + WebSocket |
| 加密 | TLS1.3 (AES-GCM 或 ChaCha20) | TLS1.3 + X25519 | ChaCha20-Poly1305 | TLS1.3 | TLS1.3 |
| 抗封锁 | UDP 易被 QoS | **看起来就是访问微软/苹果，墙最难封** | 一般，需配合插件 | 不错 | 一般 |
| 队头阻塞 | 无（QUIC 多流） | 有（TCP） | 有 | 有 | 有 |
| 0-RTT 重连 | ✅ | ❌ | ❌ | ❌ | ❌ |
| 弱网（高丢包）表现 | ✅✅ 大幅领先（Brutal 拥塞控制） | 拉胯 | 拉胯 | 拉胯 | 拉胯 |
| 好链路极限吞吐 | 中等（QUIC 软件栈瓶颈，~500M~1G） | ✅ 跑满千兆/2.5G | ✅ 高 | ✅ 高 | 中（WS 有 framing 开销） |
| **x86 CPU 占用**（有 AES-NI） | 中 | ✅ 低 | ✅ 低 | ✅ 低 | 中 |
| **MIPS/ARM 路由 CPU**（无 AES 硬加速） | ❌❌ 很高 | ✅ 低（X25519+ChaCha20） | ✅ 最低 | ChaCha20 配置低 / AES 高 | 高 |

---

## 选型决策表

### 按"运行设备"

| 设备 | 推荐 | 避免 |
|---|---|---|
| **MT7621 / 7620 等 MIPS 路由** | Reality / SS-chacha20 | Hy2、TUIC（QUIC 系软件 AES 太吃 CPU） |
| **ARMv7 / ARMv8 入门软路由**（如树莓派4） | Reality / SS-chacha20 | Hy2（除非链路确实差） |
| **x86 软路由 / N100 / Intel CPU 有 AES-NI** | 都行，按链路选 | 无 |
| **手机 / 笔记本（直连 v2rayN/Sing-box）** | 都行 | 无 |

### 按"链路质量"

| 链路情况 | 首选 | 理由 |
|---|---|---|
| IPLC / IEPL 专线、白天空闲 | **Reality** | TCP+BBR 在好链路上效率最高 |
| 普通中转、晚高峰拥堵 | **Hysteria2 (Brutal)** | QUIC 不受队头阻塞影响，丢包不掉速 |
| 4G/5G 移动网络 | **Hysteria2** | 移动网丢包高，QUIC 优势明显 |
| 运营商 QoS 限速 UDP | **Reality** | UDP 会被掐到几 Mbps，TLS over TCP 装得像 HTTPS |
| 国内大水管直连落地（韩日新港） | Reality | 跑满千兆 |
| 落地是杂 VPS、跨国绕路严重 | Hysteria2 | 抗丢包是它的本职工作 |

### 按"应用场景"

| 场景 | 推荐 |
|---|---|
| 看 YouTube/Netflix 4K（持续高码率） | Reality（稳定带宽优先） |
| Telegram/Discord 语音视频（实时低延迟） | Hysteria2（0-RTT、抗丢包） |
| Steam / GitHub 下大文件 | Reality（单连接吞吐高） |
| 浏览网页、刷推（大量短连接） | Hysteria2（QUIC 多流） |
| 外服游戏（求低 ping） | Hysteria2（抗丢包，避免 TCP 重传抖动） |
| 走 BT/PT | Reality（节省 CPU、不会被 QoS）|

---

## 实操建议

### x86 上 v2rayN / Sing-box / Clash
- **都留着，按需切换**。很多人就是"白天 Reality，晚高峰 Hy2"。
- 用 Mihomo/Sing-box 的 **fallback / urltest 策略组** 自动选最快。
- speedtest.cn 测的是 TCP，跑出来 Reality 通常更高；但实际看视频体感未必。

### KWRT 弱路由
- **首选 Reality**。Hy2 在 MT7621 上单核能干到 240% CPU，4K 直接卡。
- 不得不用 Hy2 的话，把 sing-box 的 brutal 速率限制设得保守一些（比如 30Mbps），减少 CPU 抢占。

### 协议加密的具体选择
- **能选 ChaCha20 一定选 ChaCha20**（除非你设备有 AES-NI/AES 指令集）。
- Reality 默认就是 X25519+ChaCha20，自动最优。
- SS 节点选 `chacha20-ietf-poly1305`，别选 `aes-256-gcm`。

---

## 常见误区纠正

| 误区 | 真相 |
|---|---|
| "Hysteria2 比 Reality 快" | 仅在弱网/丢包场景。好链路 Reality 通常更快 |
| "TCP 比 UDP 落后" | TCP+BBR 在好链路上效率上限更高；UDP 优势在弱网 |
| "调大 sysctl 缓冲对所有协议都有效" | **只对 TCP 系协议有效**。Hy2/TUIC 走 QUIC，缓冲全在用户态，sysctl 无影响 |
| "测速软件跑出 800Mbps 实际就是最快" | speedtest 只测单/几连接 TCP，QUIC 在多连接小请求场景体感可能反超 |
| "Reality 容易封" | 反过来，Reality 是目前**最难封**的，因为它"偷"真站点的 TLS 指纹 |
| "弱路由跑不动 Reality" | Reality 用 ChaCha20，比 Hy2 的 AES-GCM 友好得多，MIPS 也能跑 |

---

## 一些数字（在我的设备上实测）

设备：Netgear R6800 / MT7621 / MIPS 1004Kc / 880MHz 双核 4VPE

| 节点 | sing-box CPU（看 4K 时） | 体感 |
|---|---|---|
| 233boy-Hysteria2 (Xray 客户端) | 240% (吃满 2.4 核) | 卡 |
| 233boy-Hysteria2 (sing-box 客户端) | ~46% | 仍偶卡 |
| 233boy-Reality (sing-box) | 明显更低 + 极限速度上来 | 流畅 |

→ 同一台路由器、同一服务商，**协议选错代价就是 5–10 倍 CPU**。

---

## 内核选型（这是比"协议选谁"更上层的一层）

> 协议是"用什么砖"，内核是"用什么砌墙的工具"。同一份节点配置在不同内核里跑，性能、稳定性、可用性可能差几倍。

### 主流三家对比（截至 2026-06）

| 维度 | **Xray-core** | **sing-box** | **mihomo**（原 Clash.Meta） |
|---|---|---|---|
| 仓库 | [XTLS/Xray-core](https://github.com/XTLS/Xray-core) | [SagerNet/sing-box](https://github.com/SagerNet/sing-box) | [MetaCubeX/mihomo](https://github.com/MetaCubeX/mihomo) |
| 开源协议 | **MPL-2.0**（最宽松） | **GPL-3.0 + 名称保护条款** | **GPL-3.0** |
| GitHub Star（2026.6） | 39.2k | 34.6k | 22k+ |
| 主导者 | RPRX 个人主导 + 250 contributor | nekohasekai 个人主导 + 99 contributor | 社区/MetaCubeX 团队驱动 |
| 起源 | fork 自 v2fly-core，加 XTLS / Reality / Vision 等专属协议 | nekohasekai 从零重写（同作者前作 SagerNet/v2rayNG） | fork 自 Clash Premium 传统，Clash 闭源后社区接力 |
| 最新版本 | v26.6.1 (2026-06) | 1.13.12 (2026-05) | v1.19.x |
| 主语言 | Go 99.7% | Go 96.7% | Go |
| 路由器原生包 | OpenWrt（PassWall/PassWall2 自带） | OpenWrt（PassWall sing-box 客户端） | OpenWrt（OpenClash、Nikki、ShellClash） |

### 协议支持矩阵

| 协议 | Xray-core | sing-box | mihomo | 备注 |
|---|---|---|---|---|
| VLESS + Reality + Vision | ✅✅ **原生最先实现** | ✅ 实现稍滞后 | ✅ | Reality 是 Xray 团队作品 |
| VMess | ✅ | ✅ | ✅ | |
| Trojan | ✅ | ✅ | ✅ | |
| Shadowsocks | ✅ | ✅✅ **含 SS-2022** | ✅✅ | SS-2022 协议本身就是 nekohasekai 设计 |
| **Hysteria 2** | ⚠️ 实现晚且性能差 | ✅✅ **官方推荐** | ✅ | 这就是你路由器实测的现象 |
| TUIC v5 | ⚠️ | ✅✅ | ✅ | |
| AnyTLS | ❌ | ✅ | ✅ | 较新的抗封锁协议 |
| WireGuard 出站 | ✅ | ✅ | ✅ | |
| **XHTTP / Encryption** | ✅✅ **Xray 独家** | ❌ | ❌ | XTLS 团队最新方案 |

### 三家的"价值观"差异（决定你长期会不会踩坑）

**Xray-core (RPRX)**
- ✅ 协议创新最激进：Reality、Vision、XHTTP、VLESS Encryption 全是 Xray 团队推出
- ✅ 跨境抗封锁的**前沿阵地**
- ❌ **激进砍兼容性**：2026.6.1 强制删 `allowInsecure` 就是典型，老配置直接拒绝启动
- ❌ 决策由 RPRX 个人主导，社区 issue 经常被 "closed as not planned"
- ❌ Hy2/TUIC 等"非 Xray 系"协议是二等公民，实现质量差

**sing-box (nekohasekai)**
- ✅ 工程质量最高、稳定性最好、文档最完整
- ✅ 对**所有协议平等**支持，特别是 Hy2/TUIC/SS-2022 等新协议**实现就是参考标准**
- ✅ iOS / macOS / Apple 平台支持最完整（含 Apple 网络框架原生集成）
- ✅ 配置稳定，向后兼容做得最严肃
- ❌ Reality 等 Xray 专属新特性会**滞后几个月**才支持
- ❌ GPL-3.0 + 名称保护条款，商用集成相对受限

**mihomo (MetaCubeX 社区)**
- ✅ Clash 配置生态最大（规则集、订阅转换、Dashboard 全套）
- ✅ **代理组 / Provider / Fallback / URL-Test 等"策略"功能最强**，适合多节点切换
- ✅ TUN 模式、绕过国内、Mainland/Geosite 规则成熟
- ✅ 社区驱动，PR 接受度高，迭代快
- ❌ 协议实现通常**跟随 sing-box/Xray**，不是首发
- ❌ 历史包袱较重（Clash Premium 闭源后期遗留的兼容代码）

### 选型决策

| 你的场景 | 推荐内核 |
|---|---|
| 路由器跑 Hy2 / TUIC / SS-2022 | **sing-box** |
| 路由器跑 Reality / VLESS-Vision | **Xray-core**（sing-box 也行，慢半拍） |
| PC 上多节点策略切换（按地区/规则） | **mihomo** + Clash Verge Rev / FlClash |
| PC 上只跑 1–2 个节点 | v2rayN（同时带 Xray + sing-box 两个内核，会自动选） |
| iOS / macOS | **sing-box**（生态最强） |
| 想用最新抗封锁协议（XHTTP / VLESS Encryption） | **Xray-core**（独家） |
| 想要长期稳定不踩雷 | **sing-box**（最严肃的兼容性策略） |
| 自建机场 / 商业部署 | Xray-core（MPL 协议商用最友好） |

### PassWall 里的"客户端类型"含义

PassWall 节点编辑页里的"客户端类型"实际就是选**这个节点用哪个内核跑**：

| PassWall 选项 | 实际内核 |
|---|---|
| `Xray` | Xray-core |
| `Sing-Box` | sing-box |
| `Hysteria` | hysteria 官方原生客户端（已不推荐，性能不如 sing-box 实现） |
| `Trojan-Plus` | trojan 原生客户端 |
| `Xray_fw` | Xray-core（针对 fw876 的 ssr+ 包） |

**2026 年最佳实践**（基于本仓库实测）：
- Hy2 / TUIC / SS-2022 类 → **Sing-Box**
- Reality / VLESS-XTLS-Vision → **Xray**
- Trojan / VMess / 旧 SS → 两个都行，**Sing-Box** 通常更稳

### 与 allowInsecure 删除事件的关系

这件事 100% 是 **Xray-core 单方面行为**：
- Xray-core ≥ 26.x：2026-06-01 起硬禁，配置里有 `allowInsecure: true` 直接拒绝启动
- sing-box / mihomo：**完全不受影响**，`tls.insecure: true` 仍正常工作
- 这是 Xray 团队"宁断老用户也要推新规范"的典型决策风格

→ 这也反向证明了一件事：**内核选 sing-box 长期最省心**；选 Xray 要做好"老配置会被砍"的心理准备。

---

## 隐蔽性 / 抗封锁深度讨论

GFW 的检测手段大致分三类，理解了再看协议优劣：

1. **被动 DPI**：看 TCP/UDP 包头、TLS ClientHello 指纹、SNI、证书链、流量大小时序熵值。
2. **主动探测**：墙看到可疑流量后，**主动连你这个 IP:Port**，发各种探测包，看回应是否像已知代理。这是 SS、VMess、Trojan 被大规模封 IP 的主要原因。
3. **流量分析 / 机器学习**：长期统计你这个 IP 的连接节奏、包大小分布、是否长连接持续上传等等，识别"看起来不像正常用户"的模式。

### 各协议在这三关上的表现

| 协议 | 被动 DPI | 主动探测 | 流量分析 | 综合隐蔽性 |
|---|---|---|---|---|
| **VLESS-Reality** | ✅ 完美：偷真站点（如 microsoft.com / apple.com）的 TLS 指纹+证书，ClientHello 100% 真实 | ✅ 完美：墙主动探测时，sing-box **把流量真的转发给被偷的真站点**，墙看到的是正常网页响应 | ✅ 几乎无破绽 | ★★★★★ |
| **Hysteria2** | ⚠️ TLS over QUIC 看起来像普通 HTTP/3，但 QUIC 流量本身就少见，**容易被运营商当成"非常规 UDP"丢/限速** | ✅ 服务端只回应带正确密码的连接，否则伪装成普通 HTTPS | ⚠️ 持续高码率 UDP 不正常 | ★★★☆☆ |
| **TUIC v5** | 同 Hy2 | ✅ | ⚠️ | ★★★☆☆ |
| **Trojan-TLS** | ⚠️ TLS 是真的，但证书是你自己的（除非用 CDN/真站点伪装） | ⚠️ 早期版本被主动探测打穿过，新版好一些 | ✅ | ★★★☆☆ |
| **VLESS-WS-TLS over CDN**（Cloudflare 中转） | ✅ 看起来就是访问 CF | ✅ 探测打到的是 CF 边缘 | ✅ | ★★★★☆ |
| **VMess-WS-TLS** | 同上，但 VMess 头有时序特征 | ⚠️ 历史上被打穿过 | ⚠️ | ★★★☆☆ |
| **Shadowsocks-2022** | ⚠️ 纯随机字节流，看起来就是"不像任何已知协议的加密流" | ⚠️ 服务端无应答时序攻击防护，墙能识别 | ⚠️ 大流量易被抓 | ★★☆☆☆ |
| **Shadowsocks 旧版**（无 AEAD/无 2022） | ❌ 已被 GFW 机器学习识别 | ❌ | ❌ | ★☆☆☆☆（不要再用） |
| **HTTP/SOCKS 明文** | ❌ | ❌ | ❌ | ☆☆☆☆☆ |

### 关键概念解释

**为什么 Reality 这么强**：
- 传统 Trojan/VMess 都是"我自己搞个证书 + 域名"，证书指纹、域名注册时间、Web 服务器响应都和真实大站不一样，墙能识别。
- Reality 的做法是：客户端发起 TLS 握手时，**让服务器以 SNI=`microsoft.com` 转发到 microsoft.com**，墙看到的握手、证书、ALPN 全是 Microsoft 的；只有客户端用对了 X25519 公钥才会被"切换"到代理通道，否则就真的是访问 microsoft.com。**这叫"借壳"，是目前最难封的方案。**

**为什么 Hy2/QUIC 类隐蔽性中等**：
- TLS over QUIC 协议本身没问题（YouTube/Google 大量用），但**家宽用户长时间持续上传/下载 UDP 443 是异常行为**。
- 部分省市运营商会对长连接 UDP 直接限速到 1Mbps（典型如电信广东、移动宽带晚高峰）。
- 这不是"被封"，而是"被掐"——节点没死，但速度不可用。

**SS / VMess 为什么频繁被封 IP**：
- 主动探测可以低成本快速验证一个 IP 是不是 SS/VMess 服务器，确认后直接黑洞 IP。
- 这就是为什么 233boy 这种共享节点的 IP **几天到几周就会换一批**。
- Reality 不存在这个问题，因为探测打过去就是真站点响应，墙根本不能确认是代理。

### 隐蔽性按"使用场景"

| 场景 | 推荐 |
|---|---|
| 重大日期 / 敏感时段 | **Reality** + 中转（多一层隐蔽） |
| 公司 / 学校 / 酒店 Wi-Fi（可能有审计） | Reality 或 WS-TLS over CDN（看起来就是访问 Cloudflare） |
| 跨境出差、移动网络 | Hy2 / TUIC（移动网 UDP 限速少，且抗丢包） |
| 长期挂着下大文件 / BT | Reality（不引人注目，TCP 流量是基准） |
| 临时备用 / 应急 | 留一个 WS-TLS over CDN 节点，CDN IP 大墙不可能封 |

### 实操建议

- **永远不要只用一种协议**。手里至少备 Reality + 一种 CDN/WS-TLS 类节点。
- **不要长时间用同一个 IP 跑大流量**。共享节点会自动轮换，但自建 VPS 要自己注意。
- **服务端 IP 选择**：阿里云/腾讯云海外、Azure、AWS、Cloudflare WARP 落地这种"大众化"IP 比小机房 IP 安全。
- **关键时段**：宁可慢，用 Reality；不要为了快用裸 SS。

---

## 按设备类型综合选型（协议 + 内核 + 客户端 一站式建议）

> 这里把前面所有维度（协议、内核、隐蔽性、设备 CPU）合并成"拿到一台新设备直接抄"的版本。

### PC（x86 / 有 AES-NI / 算力富余）

| 角色 | 推荐 |
|---|---|
| **首选客户端** | **v2rayN**（Windows）/ **FlClash** 或 **Clash Verge Rev**（跨平台） |
| **内核** | v2rayN 自动带 Xray + sing-box 双内核；按节点协议自动用 | 
| **首选协议** | Reality（日常）+ Hy2（弱网/移动热点）+ 一个 WS-TLS-CDN 节点（备用） |
| **关键设置** | 开 TUN 模式实现全局代理；分流走 mihomo/Clash 规则；DNS over HTTPS |
| **不要做** | 不要在 PC 上还用裸 SS / VMess-TCP，没意义；不要只挂一种协议 |

> 选 v2rayN 还是 Clash Verge Rev？要"按节点直接连、协议全"选 v2rayN；要"按规则分流国内外、多机场订阅合并"选 Clash Verge Rev（内核 mihomo）。

### 路由器（KWRT / OpenWrt，尤其 MIPS / ARM 弱 CPU）

| 角色 | 推荐 |
|---|---|
| **应用** | **PassWall** 或 **PassWall2**（功能最全，本仓库实测） |
| **内核** | **sing-box**（Hy2/TUIC/SS-2022 都行）；只在跑 Reality / XHTTP 时切 Xray |
| **首选协议** | **Reality**（CPU 最低、隐蔽性最高，MT7621 实测能跑满 100M） |
| **次选** | SS-2022-ChaCha20（CPU 最低，但隐蔽性差，只在内网/翻强干扰时用） |
| **不要做** | **不要在 MIPS 路由跑 Hy2/TUIC**（无 AES 硬加速，QUIC 软件栈直接打爆 CPU，本仓库实测 xray-hy2 烧到 240% CPU 4K 必卡） |
| **关键调优** | sysctl rmem/wmem_max 4MB；UDP 转发端口范围放开；见 [路由器优化-4K卡顿-sysctl调优记录.md](路由器优化-4K卡顿-sysctl调优记录.md) |

> 强 CPU 软路由（N100 / J4125 / 飞牛 NAS 等带 AES-NI 的 x86）当 PC 看即可，协议任选。

### 手持设备（手机 / 平板）

#### Android

| 角色 | 推荐 |
|---|---|
| **首选客户端** | **NekoBox for Android (NB4A)** / **v2rayNG** / **Sing-box for Android (SFA)** |
| **内核** | NB4A 自带 sing-box + Xray 双内核；v2rayNG 默认 Xray；SFA 纯 sing-box |
| **首选协议** | Reality（省电）+ Hy2（移动数据弱网） |
| **省电关键** | 优先 Reality / Trojan / SS-ChaCha20（TCP 类 CPU 最低）；Hy2 在移动网下耗电明显，待机别挂 |
| **不要做** | 不要在 4G/5G 下长期挂 QUIC 类协议，运营商对长连接 UDP 的限速/中断比 Wi-Fi 严重；电量会让你怀疑人生 |

#### iOS / iPadOS

| 角色 | 推荐 |
|---|---|
| **首选客户端** | **Sing-box (SFI)**（免费、官方）/ **Shadowrocket**（付费、协议最全）/ **Stash**（Clash 系最精致） |
| **内核** | SFI = sing-box；Shadowrocket / Stash 是自研内核但协议覆盖等同 sing-box |
| **首选协议** | Reality（耗电低）+ 一个 WS-TLS-CDN 备用 |
| **省电关键** | 同 Android：优先 TCP 类；iOS 系统对 VPN 后台的限制比 Android 严，QUIC 长连接更容易被系统杀 |
| **特殊优势** | iOS 上 sing-box 用的是 Apple Network Extension 原生集成，比 Android 的 VpnService 性能更好、更稳 |

### 一图总览：设备 × 推荐组合

| 设备 | 客户端 | 内核 | 主协议 | 备用协议 |
|---|---|---|---|---|
| Windows PC | v2rayN | Xray + sing-box | Reality | Hy2 + WS-TLS-CDN |
| Mac | sing-box / Clash Verge Rev | sing-box / mihomo | Reality | Hy2 |
| 高端软路由 (N100/x86) | OpenClash / PassWall | mihomo / sing-box | Reality | Hy2 |
| 弱路由 (MT7621/ARMv7) | **PassWall** | **sing-box** | **Reality** | SS-2022 |
| Android 手机 | NB4A / v2rayNG | sing-box / Xray | Reality | Hy2（仅 Wi-Fi） |
| iPhone / iPad | sing-box / Shadowrocket | sing-box | Reality | WS-TLS-CDN |

### 跨设备共用配置的小建议

- **订阅链接形式选 sing-box / Clash 格式**而不是 v2rayN 专用格式：sing-box 订阅可被 NB4A、SFI、Stash、Clash Verge Rev 直接吃；v2rayN 专用格式只有 v2rayN/v2rayNG 能用。
- **节点命名带后缀**标 `[Reality]` `[Hy2]`，跨设备切换协议时一眼能选对。
- **路由器上跑主出口 + 终端设备直连节点**两条路并存：路由器一挂全屋透明代理，但手机出门切回直连节点，省路由器一跳延迟。

---

## 与本仓库其他文档的关系

- 排障过程：见 [路由器优化-4K卡顿-sysctl调优记录.md](路由器优化-4K卡顿-sysctl调优记录.md)
- KWRT/PassWall 实测字段：见 `/memories/repo/kwrt-passwall-facts.md`
- 探针脚本：`src-tauri/examples/probe_cpu_perf*.rs`
